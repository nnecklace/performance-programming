#include <iostream>
#include "PackedInteger.h"

#include <bitset>

using namespace std;

int main() {
    PackedInteger::Array<uint8_t> packed_array(10);

    PackedInteger::List packed_list(10, 61);
}
